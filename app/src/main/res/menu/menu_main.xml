<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view"
tools:context=".MainActivity">

    <group android:id="@+id/doctor" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_connect"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_connect"/>
        <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_profile"/>
    </group>

    <group android:id="@+id/mains" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_summery"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_summery"/>
        <item
                android:id="@+id/nav_log"
                android:icon="@drawable/ic_menu_gallery"
                android:title="@string/menu_log"/>
        <item
                android:id="@+id/nav_avatar"
                android:icon="@drawable/ic_menu_slideshow"
                android:title="@string/menu_avatar"/>
    </group>

    <group android:id="@+id/options" android:checkableBehavior="single">
        <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_settings"/>
        <item
                android:id="@+id/nav_switch"
                android:icon="@drawable/ic_menu_camera"
                android:title="@string/menu_switch"
                android:onClick="switchProfiles"/>
    </group>

</menu>
